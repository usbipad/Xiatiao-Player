xiatiao-player (1.0.2) unstable; urgency=medium

  * 修复切歌后歌词不显示：_apply_track_assets 引用了未定义变量抛异常，
    被 except 静默吞掉，连带进度条跟随色、主界面背景跟随一并失效。
  * 修复系统托盘 dbusmenu 方法返回值类型错误：GetGroupProperties 等
    返回值未按 DBus 规范用元组包裹，触发 GLib-CRITICAL 并可能超时。
  * 关键 UI 路径（切歌资产应用、队列刷新）的静默 except 补 debug 日志，
    便于排查；默认不输出，不产生噪音。
  * 新增统一调试入口 XIATIAO_DEBUG 与 tools/debug_run.sh。
  * 修复 GTK4 段错误崩溃（SIGSEGV in gtk_widget_unparent）：右键菜单等
    临时 Popover 只 set_parent 未 unparent，父控件销毁时访问已释放对象。
    为全部临时 popover 补解父（菜单用 closed 信号，音量用 do_dispose）。
  * 音效弹窗重构：由居中对话框改为从音效按钮旁弹出的气泡，主页/沉浸页
    共用；背景跟随主界面背景色，用高优先级 provider 压过第三方主题。
  * 音量图标支持鼠标滚轮调节（主页 + 沉浸页），图标随音量大小变化。
  * 专辑/艺术家卡片悬停浮起效果（上浮 + 阴影 + 封面轻放大）。
  * 沉浸页控制栏按钮重排（音效/随机/播放组/循环/音量）。
  * 修复左侧面板歌名/歌手不显示：MarqueeLabel 垂直最小高度报 0，
    空间紧张时被压成 0 高而隐藏；改为报实际文字高度。
  * 修复左侧播放面板外圈白边/右缘竖线（libadwaita 侧栏容器与分隔
    装饰 AdwGizmo 的描边）。
  * 窗口最小宽度 = 左侧面板完整宽度（含 margin），缩到最窄只剩面板。
  * 主界面「背景跟随封面」开启时，菜单/音效气泡背景随之变化。

 -- usbipad <1495941192+usbipad@users.noreply.github.com>  Wed, 24 Sep 2026 02:00:00 +0800

xiatiao-player (1.0.1) unstable; urgency=medium

  * 修复首页展开专辑/艺术家时内存飙升：封面改在解码阶段缩放到目标尺寸，
    避免 6000x6000 内嵌封面整张解码（单张约 144MB）。
  * 折叠视图卡片改为懒构建，不再与展开网格各建一份（封面解码与控件数量减半）。
  * 卡片轮播加守卫：折叠视图未构建或页面不可见时不再轮换，避免定时器堆积。
  * 消除窗口缩放/切页/进出沉浸页时的同步重活，提升响应。
  * 隐藏歌词区滚动条。
  * 修正构建日志残留：.gitignore 补 *.build，清理 glob 改为 _amd64*.build*。

 -- usbipad <1495941192+usbipad@users.noreply.github.com>  Mon, 21 Sep 2026 14:45:00 +0800

xiatiao-player (1.0.0) unstable; urgency=medium

  * 首个正式版本。
  * GTK4 界面 + Rust 音频后端（Unix socket IPC）。
  * 输出：PipeWire 原生（采样率跟随）+ ALSA 独占。
  * DSD 支持：Native 直通 / DoP v1.0 / PCM 软解。
  * 内嵌 CamillaDSP 音效链，参数平滑更新。
  * 曲库 / 歌单 / 收藏 / 历史，MPRIS2 与系统托盘。
  * seek 修复：PCM/DSD 软解与直通路径的定位、状态机与音效衔接。
  * 列表正在播放行高亮（与音效弹窗选中样式一致）。
  * 主界面背景跟随封面主色调（设置 → 外观，独立开关）。
  * 进程名/窗口 app_id 修正：系统监视器/htop 显示应用名而非 python3。
  * 设置窗口开关即时生效（修正 PreferencesWindow 的 get_root 误用）。
  * 打包：Debian 12 基线构建，产物 GLIBC_2.34，覆盖 Debian 12+ / Ubuntu 22.04+。
  * 依赖：全部并入 Depends，一条 apt install 装齐。
  * 清理历史遗留死代码（gst_backend/dsp_presets/dsp_page 等）。

 -- usbipad <1495941192+usbipad@users.noreply.github.com>  Sun, 20 Sep 2026 03:00:00 +0800
