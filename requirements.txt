# Xiatiao Player 运行依赖（Python 侧）。
# 注意：GTK4 / libadwaita / GStreamer 等通过系统包（apt/gir）提供，
# 不在 pip 范围内；此处只列 Python 包依赖（与 debian/control 对应）。
#
# Debian/Ubuntu 安装系统依赖：
#   sudo apt install python3-gi python3-gi-cairo python3-numpy python3-yaml \
#        python3-mutagen gir1.2-gtk-4.0 gir1.2-adw-1 gir1.2-gdkpixbuf-2.0 \
#        gir1.2-gstreamer-1.0 gir1.2-gst-plugins-base-1.0 ffmpeg pipewire pipewire-bin
#
# 下列包在多数发行版由系统提供；仅在虚拟环境/非 Debian 环境用 pip 补装：
PyGObject>=3.44   # gi（通常用系统包）
numpy>=1.24
PyYAML>=6.0
mutagen>=1.46
