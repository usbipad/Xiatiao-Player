# 项目级 cargo 配置。

# 离线构建：使用 vendor/ 目录中的依赖副本（随源码包分发）。
# 这些依赖由 `cargo vendor vendor` 生成，确保 Debian 打包时无需联网。
[source.crates-io]
replace-with = "vendored-sources"

[source.vendored-sources]
directory = "vendor"

[net]
retry = 5
